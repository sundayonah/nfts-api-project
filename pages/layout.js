import React from 'react';
import { Button, Card, Footer, Logo } from '../Components';

const layout = () => {
   return (
      <div className="home">
         <Logo />
         <Button />
         <Card />
         <Footer />
      </div>
   );
};

export default layout;
